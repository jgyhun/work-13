<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h3>폼 전송 실습</h3>
    <h4>GET요청</h4>
    <form action="/getForm" method="GET">
        이름</label><input type="text"  name="name" id="name" required>
        <fieldset>
            <legend>성별</legend>
            <input type="radio" name="gender" value="남자"/>남자
            <input type="radio" name="gender" value="여자"/>여자
        </fieldset>
        <br />
        <fieldset>
            <legend>생년월일</legend>
            <select name="year">
                <% for(let i = 1970; i <= 2024; i++){ %>
                    <option value="<%= i %>"><%= i %></option>
                <% } %>
            </select>년
            <select name="month">
                <% for(let j = 1; j <= 12; j++){ %>
                    <option value="<%= j %>"><%= j %></option>
                <% } %>
            </select>월
            <select name="day">
                <% for(let y = 1; y <= 31; y++){ %>
                    <option value="<%= y %>"><%= y %></option>
                <% } %>
            </select>일

        </fieldset>
        <br />
        <fieldset>
            <legend>관심사</legend>
            <input type="checkbox" name="hobby" value="여행"/>여행
            <input type="checkbox" name="hobby" value="패션"/>패션
            <input type="checkbox" name="hobby" value="음식"/>음식
        </fieldset>
        <br />
        <button type="submit">회원가입</button>
    </form>
    <hr />
    <h1>post 요청</h1>
    <form action="/postForm" method="post">
        이름<input type="text" required name="name">
        <fieldset>
            <legend>성별</legend>
            <input type="radio" name="gender" value="남성" />남성
            <input type="radio" name="gender" value="여성" />여성
        </fieldset>
        <br />
        <fieldset>
            <legend>생년월일</legend>
            <select name="year" >
                <% for(let i = 1970; i <=2024; i++ ){ %>
                    <option value="<%= i %>"><%= i %></option>
                <% } %>
              
            </select>년
            <select name="month" >
                <% for(let j = 1; j <= 12; j++ ){ %>
                    <option value= "<%= j %>"><%= j %></option>
                <% } %>
            </select>월
            <select name="day">
                 
                <% for(let y = 1; y <= 31; y++){ %>
                    <option value="<% y %>"><%= y %></option>
                <% } %>
            </select>일
        </fieldset>
        <fieldset>
            <legend>관심사</legend>
            <input type="checkbox" name="hobby" value="여행" />여행
            <input type="checkbox" name="hobby" value="패션" />패션
            <input type="checkbox" name="hobby" value="음식" />음식
        </fieldset>
        <fieldset>
            <legend>선택</legend>
            좋아하는 색상<input type="color" name="color"><br />
            좋아하는 숫자<input type="number" name="number">
        </fieldset>
        <button type="submit">회원가입</button>
    </form>
</body>
</html>